{
  "name": "jekyll-x3d",
  "version": "1.0.3",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -f ./*.gem",
    "build": "gem build jekyll-x3d.gemspec",
    "try": "sudo gem install ./*.gem",
    "release": "node build/release.js",
    "merge:development": "sh build/merge-development.sh"
  },
  "devDependencies": {
    "glob": "^13.0.6",
    "shell-tools": "^1.1.9"
  }
}
